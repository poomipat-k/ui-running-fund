import { ReviewDetails } from './review-details';

export class ReviewerImprovement {
  benefit?: boolean;
  experienceAndReliability?: boolean;
  fundAndOutput?: boolean;
  projectQuality?: boolean;
  projectStandard?: boolean;
  visionAndImage?: boolean;

  constructor() {}
}
